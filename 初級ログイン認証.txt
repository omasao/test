*DBConnector*

17 (Connection)の意味
→誤植。getConnectionはConnection型を戻り値として返すので不要。

*sql*
全体的にわからない単語がある

*home.jsp*
8 metaの意味→検索上位にさせるため
34 #topなど→CSSのIDセレクタ

*login.jsp*
71 <s:form action>のsの意味
76　<br/>の/の意味→XHTMLの文法？
78　<span>は使っていないが？

*loginDTO*
8 Flg=false とは？

*LoginDAO*
21 PreparedStatement preparedStatement=connection.prepareStatement(sql);
この文のconnectionは15の変数connectionだと思うが、変数のprepareStatementというメソッドを実行するとはどういうことか？
大文字のインターフェイスConnectionではないのか？

インターフェイス、クラスライブラリの単語をを理解したらより理解できると思う

29 loginDTO.setLoginId(resultSet.getString("login_id"));
メソッドの中にメソッドを書くこともできる？

*LoginAction.java*

*UserCreateCompleteDAO*
DateUtilとは

*UserCreateCompleteAction*
19 toStringとは


